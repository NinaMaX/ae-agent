env/
venv/
__pycache__/
*.pyc
.env
.DS_Store

# Symlinks Streamlit auto-creates into its own bundled agent-skill file
# inside env/ - environment-local, would dangle for anyone else's venv.
.agents/
.claude/

# Runtime output from using the app, not source - would just accumulate
# noise in git history.
feedback_log.jsonl
